# The KKP Admin Dashboard runs standalone on in-memory sample data —
# no environment variables are required for `npm run dev` or the build.
#
# When wiring a real backend later, add its base URL here and read it in
# src/services/apiService.ts, e.g.:
# VITE_API_URL="https://your-backend.example.com"
